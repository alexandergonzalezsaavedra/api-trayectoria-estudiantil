import React from 'react'
import Endpoints from '../../../00-Endpoint/Endpoints/Endpoints'

const PesumProgramas = ({
    estadoCarga,
    autenticacion,
    estudiante,
    resProgramas,
    setResProgramas,
    programasEstudiante,
    setProgramasEstudiante,
    resSemestres,
    setResSemestres
}) => {
    return (
        <div>
            {autenticacion}
        </div>
    )
}

export default PesumProgramas
